def are_you_playing_banjo(name):
    # method whether a string starts with a capital letter
    if name.startswith('R') or name.startswith('r'):

        return name + " plays banjo"
    else:
        return name + " does not play banjo"
